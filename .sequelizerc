const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'src', 'config', 'database.js'), //local aonde ira ficar o arquivo de config
    'migrations-path': path.resolve(__dirname, 'src', 'database', 'migrations')
}